<form [formGroup]="reportForm" (ngSubmit)="onSubmit()">
  <mat-card>
    <mat-card-title>Report Editor</mat-card-title>
    <mat-card-content>
      <mat-form-field appearance="fill">
        <mat-label>Report Name</mat-label>
        <input matInput formControlName="reportName">
      </mat-form-field>

      <mat-form-field appearance="fill">
        <mat-label>Report Date</mat-label>
        <input matInput [matDatepicker]="picker" formControlName="reportDate">
        <mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>
        <mat-datepicker #picker></mat-datepicker>
      </mat-form-field>

      <div formArrayName="entries">
        <div *ngFor="let entry of entries.controls; let i=index" [formGroupName]="i">
          <mat-divider></mat-divider>
          <h3>Entry {{ i + 1 }}</h3>

          <mat-form-field appearance="fill">
            <mat-label>Procedure</mat-label>
            <input matInput formControlName="procedureName" [matAutocomplete]="auto">
            <mat-autocomplete #auto="matAutocomplete">
              <mat-option *ngFor="let option of filteredProcedures[i] | async" [value]="option.name">
                {{ option.name }}
              </mat-option>
            </mat-autocomplete>
          </mat-form-field>

          <mat-form-field appearance="fill">
            <mat-label>Start Time</mat-label>
            <input matInput [matDatepicker]="startPicker{{i}}" formControlName="startTime">
            <mat-datepicker-toggle matSuffix [for]="startPicker{{i}}"></mat-datepicker-toggle>
            <mat-datepicker #startPicker{{i}}></mat-datepicker>
          </mat-form-field>

          <mat-form-field appearance="fill">
            <mat-label>End Time</mat-label>
            <input matInput [matDatepicker]="endPicker{{i}}" formControlName="endTime">
            <mat-datepicker-toggle matSuffix [for]="endPicker{{i}}"></mat-datepicker-toggle>
            <mat-datepicker #endPicker{{i}}></mat-datepicker>
          </mat-form-field>

          <mat-form-field appearance="fill">
            <mat-label>Comment</mat-label>
            <input matInput formControlName="comment">
          </mat-form-field>

          <button mat-icon-button type="button" (click)="removeEntry(i)">
            <mat-icon>delete</mat-icon>
          </button>
        </div>
      </div>

      <button mat-button type="button" (click)="addEntry()">Add Entry</button>
    </mat-card-content>
    <mat-card-actions>
      <button mat-raised-button color="primary" type="submit">Save</button>
    </mat-card-actions>
  </mat-card>
</form>
