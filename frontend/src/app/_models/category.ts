export class Category {
    id: number;
    mainGroup: string;  // Основные группы
    costGroup: string;  // Группа затрат рабочего времени
    costCategory: string;  // Категория затрат рабочего времени
    costIndex: string;  // Индекс элемента затрат рабочего времени
    costName: string;  // Наименование затрат времени
  }
  